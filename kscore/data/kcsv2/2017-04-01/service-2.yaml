---
version: '2.0'

metadata:
  apiVersion: '2017-04-01'
  endpointPrefix: kcs
  jsonVersion: '1.1'
  serviceFullName: KCS Service
  signatureVersion: v4
  targetPrefix: kcs
  protocol: kcs

operations:
  # 添加只读实例
  AddCacheSlaveNode:
    name: AddCacheSlaveNode
    http:
      method: PUT
  # 删除只读实例
  DeleteCacheSlaveNode:
    name: DeleteCacheSlaveNode
    http:
      method: PUT
  # 查询缓存服务只读实例列表
  DescribeCacheReadonlyNode:
    name: DescribeCacheReadonlyNode
    http:
      method: GET